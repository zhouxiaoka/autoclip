# AutoClip 1.4 发布计划与验收记录

更新时间：2026-09-22（北京时间）。目标：本周末上线，按 **9 月 25 日周五形成 RC、9 月 26 日周六回归、9 月 27 日周日发布窗口**推进。日期是工作目标，发布仍以验收结果为准。

## 本版交付

视觉理解驱动的游戏高光与推广成片，是 1.4 的核心功能。用户在 AutoClip 中配置视觉模型，导入素材、查看候选、编辑、保存语言/开头变体、预览与导出。原内容切片和合集继续可用。

首发聚焦真实录屏、人工可修的创意流程。**不仅以能导出作为质量完成标准**：至少一个游戏方向达到 L2（可进入小规模投放测试的创意与制作质量），再扩大素材覆盖。L3 起量/ROI 需要真实投放数据，不以模型分数、播放量或活动整体成绩替代。

## 截至本次已完成

- [x] 统一导入、结果页、轻量编辑页和导出记录。
- [x] 视觉接口独立配置、图片理解连接测试、环境默认配置兼容。
- [x] 有序静帧初筛与第一候选密集复核，保存事件证据和时间边界。
- [x] 文案、文字语言、镜头起止与顺序、原声、字幕、画幅和构图可配置。
- [x] 真实 FFmpeg 渲染、不可变导出快照、版本冲突保护、重启后的失败提示。
- [x] 另存当前编辑内容为独立语言/开头版本，保留父稿与导出，记录父稿 ID 和版本。
- [x] 一次真实 Seed 联调：22 秒跑酷素材、2 个事件、3 个开头、1080×1920 成片。

## 剩余工作，按优先级执行

**最新用户顺序：先将首期六款模板优化到已确认漫画版的质量并逐款复看，再推进卡拉 OK 字幕。** 漫画 v5 已得到用户认可；统一 v6 中漫画字层不变，其余五款本轮提交复看。在六款视觉确认前，不继续扩展字幕动效。

| 优先级 | 项目 | 验收证据 | 目标 |
| --- | --- | --- | --- |
| P0 | 创意成片质量 | 跑酷方向基础片 + 两个有明确开头假设的变体；目标/行动/反馈清楚；合理收尾；文字不挡玩法；声音无突断/削波；与参考片及人工样片逐项对照 | 9/23–24 |
| P0 | 文字本地化与排版 | 中/英/日长短文本、横竖源画幅；最终烧录文字不溢出、不遮关键 HUD；明确区分源预览与实际成片 | 9/23 |
| 已完成核心链路 | 候选替换与修正 | 可在当前成片中换入/追加真实候选；重排、裁切、保存、重新导出后时间与画面一致 | 9/23–24 |
| P0 | 失败与资源控制 | 模型超时/错误密钥、下载失败、无有效事件、缺字幕、连续重试、删除项目、服务重启；任务可理解、可恢复，旧成片可下载 | 9/24 |
| P0 | 样本评测 | 跑酷、合成、驾驶至少各一个样本；记录漏检、错误边界、人工修改时间、耗时/用量；不合格方向明确标注或不宣传支持 | 9/24 |
| P0 | 用户成片验收 | 至少一组成片提交用户复看；按 GAME_AD_CREATIVE_QUALITY.md 的硬性问题与 L2 标准记录结论；无法取得第二评审时标明单人评审，不能代填通过 | 9/24–25 |
| P0 | 合并与桌面回归 | 和主工作区已完成的更新器、Sentry 等改动整合后重跑关键流程；macOS ARM64 与 Windows x64 安装包、无开发依赖启动、中文字体与 ffmpeg 验证 | 9/25–26 |
| P0 | 发布准备 | 统一 1.4.0 版本、变更记录、配置说明、兼容/迁移说明、安装包与更新清单、可回退版本 | 9/25–26 |
| P1 | Golden 案例持续研究 | 完整播放可审阅参考；具体视频效果/规模证据分别登记。证据不足保留候选，不伪造 golden_verified | 并行研究 |
| P1 | Jev 对照实验 | 先确认用户所指模型与真实接口，再对相同事件集比较耗时、质量与成本，有改善再接入 | 不作为首发依赖 |
| 后续 | 批量投放闭环、更多结构/游戏、生成镜头、完整时间线 | 在 1.4 样片与使用数据基础上安排 | 1.4.x / 后续版本 |

声音与收尾以素材和玩法为依据；不为了赶进度自动添加无授权 BGM、虚构失败或伪造玩法。若完整包装尚未达到 L2，不能把“高光初剪”描述成已完成买量素材系统；必须在发布评审中明确缺口。

## 每日推进

- **9/22 周二：** 固定首发范围、完成版本分支能力、补核心接口测试，建立发布清单。
- **9/23 周三：** 文字排版与构图、替换镜头、成片结构，完成首轮多样本评测。
- **9/24 周四：** 依据样片复看修正质量问题，提交基础片和变体供用户验收；补失败路径。
- **9/25 周五：** 停止增加新范围，整合已完成主线改动，准备 1.4.0 RC 与说明。
- **9/26 周六：** 安装包、升级与旧项目回归，修复发布阻塞问题。
- **9/27 周日：** 汇总通过/未通过证据，符合门槛后进入发布步骤；未通过项明确报告及建议，不以时间到达视为验收完成。

## 工作隔离与发布边界

所有本功能开发继续在 `/Users/zhoukk/.codex/worktrees/visual-game-ads/autoclip` 的 `codex/visual-game-ads` 分支。原 `/Users/zhoukk/autoclip` 有未提交的更新器、监控和文档等改动，不能 reset、覆盖、盲目 cherry-pick 未提交状态或提前合并。

当前功能分支桌面/后端版本仍为 1.2.1，前端包版本为 0.0.0；在 RC 整合阶段统一到 1.4.0，并检查版本读取来源及更新清单。不能提前打 `v1.4.0` tag：现有 CI 会因 `v*` tag 自动创建发布与上传产物。

已授权持续进行实现、回归、修复和功能分支提交。到发布步骤先提交具体候选产物及验收结论，再按当时用户授权执行外部发布。没有真实完成安装包验证或发布时，不标记“已上线”。

## 持续推进记录

### 2026-09-22：独立成片版本

新增“另存为新版本”，可复制尚未保存的编辑内容并指定输出语言。复制不修改原稿、不拷贝原稿导出记录、不重新分析视频；原稿已在另一窗口更新时，也可将旧窗口编辑另存以保留工作。未来版本号、错误来源、越界镜头与空标题均拒绝。

验证结果与后续完成项在每轮实际工作后更新本文件，保留具体失败和未验证项。此前真实联调证据见 [统一工作区实现说明](UNIFIED_STUDIO_IMPLEMENTATION.md)。

本轮验证：后端 25 项、前端 9 项测试通过；类型检查、ESLint 和生产构建通过（保留现有包体积提示）。浏览器完成未保存文案另存英文版、返回项目查看双版本、重开原稿确认文案/语言与旧导出仍保留。未进行安装包验收或发布。

已启用当前任务的自动推进：北京时间每日 09:00、15:00、21:00，截至 9 月 27 日；仅在有意义的里程碑、失败或需要决定时通知。自动推进 ID：`autoclip-1-4`。


### 2026-09-22：候选镜头与成片链路接通

新增项目候选接口和编辑器选择面板，支持原片预览、替换、追加、撤销、保存及重新渲染；同时添加源时长和 30 镜头前端校验，后端仍校验真实文件边界。空文件导入给出 422 提示，导出请求携带当前草稿版本防止跨窗口错稿。视觉分析增加真实执行阶段提示。

自动检查：后端 30 项、前端 11 项通过，包括修改镜头后真实渲染的顺序和时长；类型检查、ESLint、生产构建通过。原片字幕、语言和独立版本能力沿用现有接口，不重新分析画面。浏览器使用实际应用工厂、隔离数据库和用户配置的真实 Seed 接口，完成 22 秒跑酷视频导入、分析、自动显示 3 个事件与 3 份草稿；替换为 8–14 秒、追加 16–20 秒，验证保存、保存后撤销再保存、刷新恢复与切换英文输出。页面触发真实英文翻译与后台渲染，产出 10.02 秒、1080×1920、30 fps H.264/AAC MP4（约 4.88 MB）；浏览器切换到实际渲染预览、收到下载事件，项目卡片和导出历史均显示 V4/English/2 段。提取帧确认中文开头已翻译为英文。下载工具等待时间异常偏长，不能将该工具延迟直接归因于应用。

此次联调样片验证接口和状态一致性，不作为 golden case：默认完整画面构图仍有大面积留边，英文标题分行与视觉包装需要继续优化。浏览器为 macOS 内嵌浏览器，未覆盖 Windows 安装包。

首轮真实分析在默认 180 秒请求窗口发生超时。补充中文可操作错误、保留原素材，使用设置页将本次隔离联调配置改为 300 秒后重试成功，分析整体约 270 秒。文案修改前端等待窗口同步覆盖可配置最大值，避免提前中断。仍需优化模型响应耗时、评估分阶段结果复用；该结果不代表买量质量或安装包发布验收通过。

### 2026-09-22：修正竖屏构图与标题模板

根据用户反馈，先前 1080×1920 只是竖屏容器，`fit` 把横屏主体缩在中间；原始 drawtext 标题没有形成可用包装。新生成的竖屏视觉草稿改为默认满屏裁切，新增 0–1 横向取景位置；编辑器提供“应用竖屏推荐”供已有草稿明确切换，保留旧版 fit 和导出文件以兼容历史项目。当前是固定取景位置，不宣称自动跟踪主角。

新增“高能大字”（黄色粗体、深色描边、居中分行）和“简洁标题卡”（深色底、薄荷绿强调线、左对齐），模板跟随草稿与导出快照保存。英文按词分行、中文按字宽分行，最多三行、逐级缩字号，过长明确拒绝；纯文本通过文件输入并关闭 drawtext 展开，保持特殊字符安全。首帧即显示标题。原始基础文字模板保留。

后端 34 项、前端 12 项测试通过；类型、Lint、生产构建通过。真实像素回归验证 9:16 输出底部仍是源画面、不同横向位置选取不同区域。浏览器使用旧 V4 草稿应用竖屏推荐，保存 V5 并生成实际满屏预览。另生成两款 10.02 秒 1080×1920/30 fps 跑酷样片；每秒抽帧确认这段素材主角在画面内。中英文样片标题可读、无大面积上下黑边。其他游戏的 HUD、日文及跨平台字体仍需发布前检查；尚未标记 L2 验收通过。

### 2026-09-22：设计稿的产品化文字模板

新增漫画冲击、荧光挑战、竞技斜切三款真实模板。内置 Barlow Condensed / Noto Sans SC 与 OFL 许可；Pillow 生成透明字层、FFmpeg 负责合成和短入场动作。编辑预览、样式缩略图和导出共用绘字实现，模板参数与版本保存进草稿及导出快照。新推广草稿默认漫画冲击，旧模板保持兼容。

已生成三条同素材 10 秒样片及并排对比；浏览器验证切换、真实图层、字号/位置调整、保存 V6、刷新恢复和实际渲染。后端 40 项、前端 12 项检查；包括三款预览/导出像素一致性。wheel 构建及字体/许可打包验证通过。完整方案和仍未完成的三款模板见 [文字模板实现说明](TITLE_TEMPLATE_IMPLEMENTATION.md)。标题样式改进不等于整体买量质量验收；Windows/macOS 安装包仍未验收。


### 2026-09-22：修复重排镜头后的原声丢失与拼接边界

复现了真实渲染缺陷：3 秒画面只有前 0.6 秒带音轨，先选 2–2.5 秒无声区间，再选 0.1–0.6 秒有声区间，旧版导出没有任何音频流。现在各段统一生成 48 kHz 双声道原声，超出源音轨的区间补静音；中间文件使用 PCM，最后一次性编码 AAC，避免片段音轨结构不同导致后段声音丢失。保留源音轨自带的延迟，不把延迟后的声音提前。

每段按 30 fps 的整帧时长对齐音画，切入/切出加入 8/12 ms 短淡化以降低硬切波形突变；无音轨素材保持无声并给出提示。PCM 中间文件增加临时磁盘使用（48 kHz 双声道 16 bit 约 11.5 MB/分钟音频），随渲染临时目录清理，不保留到项目。旧版 12 段测试发现视频起始偏移约 21 ms，未复现逐段累积漂移，不能据此声称修复了累积漂移。

隔离数据回归共 44 项后端检查通过，新增真实 FFmpeg 检查覆盖无声段后原声保留、12 段拼接的边界能量与音画时长、源音轨延迟和无音轨素材；未调用付费接口。真实跑酷素材复用 8–14 秒及 16–20 秒，带漫画标题导出为 1080×1920 H.264/AAC；ffprobe 确认音频、视频均从 0 开始、均为 10.000 秒，提取帧确认标题合成正常。审阅样片为当前任务产物目录的 `title-template-review/comic-audio-review.mp4`。

这些证据覆盖声音保留与切点短淡化，不代表响度、所有原素材削波、创意节奏或 L2 买量质量验收通过；仍需多游戏样本、完整视听复看及桌面安装包回归。


### 2026-09-22：对照设计图批量调整四款字体预设

按用户最新反馈完成漫画冲击、荧光挑战、竞技斜切 v2，新增极简大字。主要修正标题偏小、外轮廓与阴影弱、底板层次不足；保留 v1 并在编辑器提供原版/加强版切换，版本进入缩略图、预览与导出链路。没有修改既存草稿或覆盖历史导出。

已生成四条加强版、三条原版对照（同素材 10 秒竖屏）及四款中文静帧，附逐款审阅页和可复现批量脚本。后端 53 项、前端 12 项检查与类型/Lint/构建通过；原版中英文六组像素与前次提交完全一致。浏览器核对审阅页原版切换、中文静帧、新增极简预设及下载目标。此轮等待用户逐款确认视觉效果；中文动态成片、多游戏遮挡和买量 L2 仍未验收。详见 TITLE_TEMPLATE_IMPLEMENTATION.md。


### 2026-09-22：字面光泽与像素/磨砂预设补齐

按用户新反馈增加 v3 材质层次（反光渐变、亮边与暗边），完成像素街机和真实局部磨砂字幕卡，六款均接入编辑器、预览和导出。像素字体及许可已内置；中日文使用网格化字体回退，仍需用户视觉确认。磨砂随当帧视频变化并在 4 秒后一起消失，不依赖 HyperFrames Studio。

首轮真实运动样片发现磨砂遮罩时间基和 YUV 色度合成问题，修正为统一帧率/时间基及 RGB 遮罩，并补运动帧、内外范围和消失边界回归。保留 v1/v2，14 组历史图层像素完全一致。批量输出六条英文视频和两条新增模板中文视频，附六款审阅页与上一轮平色对照；未调用付费模型，未发布。实际回归结果在本条末尾补记。

最终检查：后端 69 项、前端 12 项通过；类型、Lint、生产构建与字体 wheel 资源哈希检查通过。浏览器确认六款列表、平色对比入口和磨砂中文视频切换。样片待用户复看，Windows/macOS 安装包仍未验收。

### 2026-09-22：漫画字形校准与真实词时间基础

用户认为上一轮光泽版未还原设计图，本轮聚焦漫画单款 v4，换用内置 Anton、重做字面倾斜/高宽比例和硬描边，生成原设计/v3/v4放大对比及中英文各一条10秒实际成片。旧模板版本保留，新漫画草稿和预设选择使用校准版；其余款仍待逐项复看，不能标为设计验收通过。

本地 Whisper 新识别增加真实词时间旁路文件；字幕解析优先消费通过哈希、时间和全文校验的 ASR 数据，旧字幕仍标记为估算时间。改字/翻译使旧词时间失效。同步修正显式选择语言时枚举值传递。卡拉 OK 仍待镜头时间映射、渲染/预览与设置开关；实际 ASR 对齐精度和耗时尚未测量，当前只完成无模型调用的集成回归。

验证：84 项后端测试、12 项前端测试、类型/Lint/生产构建通过；wheel 内9份字体/许可资源哈希通过，包含新增 Anton。浏览器验证中英文切换及下载目标，并打开前后两版视频审阅。审阅地址本机8777，产物 `title-v4-review`。未调用付费接口、未验收安装包或发布。下轮优先完成词时间到剪辑时间的映射与首个可用字幕动效，再做口播真实样本验证。

### 2026-09-22：漫画冲击的小幅强化复看

根据“好很多了，但设计图效果更强”的反馈，在 v4 上增加可选 v5“冲击加强版”：第二行纵向放大、收紧行距、增加字重和整体倾角，扩大两侧爆点；不追加金属反光。生成中英文各10秒实际样片、原设计/v4/v5放大对比，审阅页本机8778。v4 仍是默认，v5 可在编辑器“调整文字样式 → 样式版本”选择，待用户复看。

验证：86项后端、12项前端及类型/Lint/生产构建通过；v4 六组中英日文横竖屏字层与前次提交逐字节一致。最终字重和装饰边界微调后补跑17项图层/导出一致性及边界检查通过。没有调用模型或改动主工作区；卡拉 OK 后续项维持此前计划。


### 2026-09-22：首期六款统一优化，卡拉 OK 后置

根据用户明确调整的顺序，本轮专注模板。统一 v6：漫画复用已确认 v5；荧光挑战换粗凝缩字形与饱和底板；竞技斜切改字面斜体、字重及近黑底板/蓝色侧翼；像素街机提高字面比例、收紧词间距、重做莓红描边及斜角框；极简大字改高窄奶白粗字和嵌入基线的朱红块；磨砂字幕卡加重字形、调整内距和玻璃边缘，保留真实局部动态模糊。统一进入缩略图、设置、预览、草稿与导出；新选择使用 v6，旧草稿不迁移，六款均可切回历史版本。

完成12条中英文实际成片，每条10秒、1080×1920/30fps、H.264/AAC，并逐条 ffprobe 核对音视频时长和格式；附每款原设计/上一轮/本轮放大对比与统一审阅页 `title-six-review`（本机8779）。中文六款均检查了实际烧录帧，不再只提供中文静态排版。漫画已确认，其余五款仍需用户视觉复看；没有把模板完成等同于买量 L2 验收。

首轮回归发现磨砂卡横屏大字号时内边距挤占字面，已改为同时按高度约束内距，相关15项复查通过；最终102项后端、12项前端检查及类型/Lint/生产构建通过。覆盖中英日文横竖屏边界、六款保存与预览、真实渲染字层一致性、磨砂运动同步和4秒消失边界。未调用付费模型、未改主工作区、未进行外部发布。本轮没有推进卡拉 OK 代码。
