# AutoClip 1.4 集成候选审阅

日期：2026-09-22。结论：代码集成与本机核心回归通过，可以审阅；尚不等于1.4 RC或买量素材质量验收。

## 分支与范围

- 基线：`main@336abd57`；功能：`codex/visual-game-ads@f77fc79a`。
- 集成分支：`codex/1.4-integration`，目录 `/Users/zhoukk/.codex/worktrees/1.4-integration/autoclip`。
- 主工作区未改动；未跟踪素材和 `docs/PROJECT_REVIEW_2026-09-21.md` 保留。功能分支原有15份样片源文件与主目录同名未跟踪文件逐字节一致；没有新增提交本机视频、模型密钥或运行数据。正式切换主目录前仍需复核未跟踪文件状态。
- 保留已发布1.3.1版本号，不创建触发发布的tag。没有合入main或推送远端。

## 冲突处理

| 范围 | 集成结果 |
| --- | --- |
| SQLite | 文件库NullPool独立连接 + WAL/busy_timeout；内存库StaticPool；PostgreSQL配置保留 |
| 首页与项目 | 新导入确认与统一成片入口；保留主线已有状态/操作翻译，旧片段支持转可编辑草稿 |
| 设置 | 文本模型、转写、视觉模型、应用与反馈共存；主线隐私/更新能力保留 |
| 路由/API | `/import/:id`、`/project/:id/studio/:draftId`与原路由共存；原API观察及导出中断信号兼容 |
| 依赖 | 保留Sentry、i18next、Tauri更新器；清理无引用的旧UI依赖，离线生成锁文件并独立npm ci |
| 旧组件 | 保留功能分支已批准的无用投稿弹窗/旧API清理；主线对这些文件只有翻译改动，现存业务代码无引用 |
| 资源与文档 | wheel字体资源声明与桌面递归复制兼容；保留八语README、Discussions/Projects链接及新制作指南 |

## 验证证据

- 后端155项：智能导入27项、Studio/真实FFmpeg、普通发布导出、字幕与词时间、数据库3项、Sentry/更新器/设置等。先执行154项组合，再补WAL测试并复查数据库3项；无失败，未重复运行无变化的长渲染组。
- 前端39项：主线27项与功能12项全部保留并通过；TypeScript、ESLint、Vite生产构建通过。仍有已有包体积提示。
- 文档：8份README与11份指南的链接/一致性检查通过。
- 浏览器：8781独立数据副本，检查首页、视觉设置、主线应用隐私、5分48秒素材的6个独立高光、编辑/保存/竖屏推荐/导出、旧切片转草稿。
- 导入失败恢复：本轮预览无模型密钥，本地22秒文件可导入，确认页明确没有自动推荐；勾选高光+推广显示2类，未确认前草稿和制作任务均为空。模型正常推荐/确认后分发由mock回归覆盖，本轮没有重新执行真实Seed理解。
- 实际渲染：`2774a912-8c59-4a39-b802-5e3a32f6219c`中的跑酷候选，保存中文漫画v6与竖屏，导出任务`38374fc6f278414396bec1ba0f01c511`完成。ffprobe：H.264/AAC，1080×1920，音视频起点0，时长均26.700秒；检查了实际烧录帧。这是集成烟测，不是L2创意验收。
- 旧切片兼容样本是手工样本，只证明转草稿和编辑入口；不作为旧流水线真实AI重新出片的证据。

本机审阅：<http://127.0.0.1:8781/>。数据位于 `/tmp/autoclip-integration-preview/`，不纳入git；原8780演示保持不变。新渲染文件：`/tmp/autoclip-integration-preview/data/projects/2774a912-8c59-4a39-b802-5e3a32f6219c/output/studio/38374fc6f278414396bec1ba0f01c511.mp4`。

## RC前仍需完成

1. 新增Studio文案接入八语资源，验证语言切换；现有翻译测试仅检查已经接入的静态t调用，不能发现所有硬编码中文。
2. Studio导入/确认/制作/导出的新接口补业务统计。旧API的统计、隐私关闭和Sentry保留，但新流程未形成完整出片成功/失败闭环。
3. 候选过滤/排序、长素材耗时、至少三类游戏真实样本、完整成片的创意/声音/遮挡L2验收；当前6个候选含2个奖励/菜单事件。
4. macOS ARM64和Windows x64安装包、无开发依赖启动、字体/FFmpeg、升级与回退；浏览器无法验证原生更新安装。
5. 用户审阅后整合后续功能提交，统一1.4.0版本、发布说明和更新清单，再按用户授权合入main/外部发布。卡拉OK保持后置。


## 后续集成：玩法筛选（2026-09-22）

同步功能分支`63c0849d`：先过滤模型明确标注的菜单/独立领奖/加载事件，再按观看价值保留最多6个独立高光。密集复核针对筛选后首选，不再默认使用原视频最早事件。保留未提供分类的旧接口、既存草稿与导出；筛选依据进入coverage元数据，不修改Scene/草稿存储协议或前端资源。

功能分支本轮126个不同离线用例通过（125项组合 + 新增唯一候选被否定的1项，筛选13项已复查）。真实Seed扫描约189.72秒后以RuntimeError失败，未产生新候选、未重试；未保存具体错误消息，不能确定是超时还是其他接口问题。新策略实际改善尚待验证，不能据此前测试/旧六候选宣称质量已提升。8780/8781预览保持上一轮运行状态与数据，不自动重跑用户项目。本轮不改动模板、前端和原生安装包，沿用已有相关检查，新增集成回归见发布记录。

集成针对性回归43项通过：新筛选13项、智能导入27项、数据库3项。前端代码、依赖和模板资源未改动，未重复执行不受影响的构建。


## 后续集成：视觉失败恢复（2026-09-22）

同步`cee6a0f4`。修复同时确认高光和推广、首轮视觉分析失败后再次扫描的问题；一次确认内共享一次分析尝试及其失败，独立内容切片仍可处理，推广文案失败不丢弃高光成片。结构化安全诊断写入导入计划/制作状态，包含错误码、阶段、耗时和可选HTTP状态，排除密钥、URL、请求内容和供应商原文。输出截断不再被误认为完整JSON成功；不加入后台付费重试。

功能分支相关71项离线回归通过；本轮无付费模型验证、无前端/模板/依赖改动。上轮189.72秒真实请求失败的根因仍未知；候选质量、长视频耗时、八语、业务统计及安装包验收继续待办。两条开发分支已按用户要求建立origin跟踪，本轮提交也同步备份；main未合并，未打tag或发布。8780/8781现有进程没有热重载本轮后端修改，后续真实评测需使用最新分支启动隔离服务。

本轮集成回归66项通过（故障分类20、智能导入30、筛选13、数据库3），全部使用隔离数据与离线桩。前端、模板和原生包未变，未重复无关构建。


## 后续集成：草稿、成片与版本下载（2026-09-23）

同步功能提交`5427c18b`与`e6a403fb`：结果卡片显示待导出/排队/导出中/可下载/失败/修改后需重新导出，匹配草稿ID与保存版本。保留同版成功下载及旧版历史，避免重新导出失败遮蔽可用成片。已导出缩略图使用真实文件并完整保留竖屏画面，修复横向卡片裁掉文字包装的问题。

验证：集成前端43项通过，TypeScript、Lint、生产构建通过；缩略图修复后重跑类型/Lint/构建。8781实际页面验证5条待导出草稿与1条V2已导出视频的不同状态，下载链接对应既有26.7秒任务`38374fc6f278414396bec1ba0f01c511`，截图确认竖屏文字完整展示；进入同一编辑器会恢复既有本机未保存修改，本轮未保存、覆盖或重新导出该项目。失败/排队/跨版本状态由离线测试覆盖，未伪造真实任务数据。

仍待完善：旧切片/合集展示尚有差异，新增页面八语与业务统计、真实多游戏与L2质量、安装包验收继续待办。浏览器观察到个别原片视频缩略图空白，下一轮排查元数据预载/seek及失败反馈，不将其记录为全部缩略图通过。本轮未调用付费模型、未改后端或重新渲染；已有后端预览进程仍非最新失败诊断代码，后续模型验证需重启隔离服务。主工作区保持原状，未合并main、打tag或发布。


## 最新main rebase与合入准备（2026-09-23）

按用户要求，以`origin/main@d869e284`（含v1.3.2和Whisper平台修复）重做`codex/1.4-integration`的保留合并拓扑rebase。rebase前完整备份为`codex/backup-1.4-before-rebase-20260923@ad3d183f`，功能分支`codex/visual-game-ads@e6a403fb`保留原历史。沿用已审阅的旧集成净变更，再处理最新main重叠的App、ClipCard、ProjectDetailPage、SettingsPage四处兼容；原Studio、字体模板、数据库修复与rebase前完全一致。

保留主线发布三条路由、B站/海外发布、自动封面、实时模型列表、UpdateProvider及隐私/统计/Sentry；同时保留导入确认、视觉设置、统一编辑和旧文件导出。旧切片卡片可制作、下载、发布、导出；没有旧切片的纯Studio项目不显示只能处理旧切片的发布入口，Studio导出文件自动接入社交发布尚未完成，不宣称全链路发布统一。无调用的旧首次引导模块继续保持删除。版本仍随main为1.3.2，本轮不提前发布1.4。

验证：隔离数据后端完整412项通过（包括真实FFmpeg渲染测试），前端57项、类型、Lint、生产构建通过；8份README/11份指南和社区反馈脚本检查通过。CI前端改为npm test，覆盖所有原测试及新增Studio测试。已有构建体积提示与Python弃用警告保留，不作为新失败。

浏览器使用新后端与数据副本`http://127.0.0.1:8782/`，确认视觉/发布/封面设置共存、旧手工切片可进入主线发布页、游戏V2草稿与已完成成片仍可用。未保存配置、调用付费模型或向外投稿；旧8780/8781数据不动。本地主工作区main只快进至d869e284，未跟踪artifacts与复盘文档保持原状。

合入候选可审阅，但发布验收未通过：新Studio八语/业务统计、旧结果统一、多游戏真实质量、原片缩略图可靠性、双平台安装升级等继续沿原计划推进。PR用于代码审阅和远程CI，不代表RC或买量质量达标。


## 9月23日后续：CI真实渲染与Studio发布衔接

远程Linux后端暴露缺失FFmpeg：28项失败、58项跳过，不能沿用本地412项通过宣称CI通过。CI现显式安装FFmpeg/ffprobe及中文字体，恢复完整真实渲染检查；前端和Docker原检查已通过。

Studio完成文件作为不可变发布来源接入现有B站/海外发布与封面服务，结果卡片、编辑器导出面板和导出记录均可进入同一发布页。不改写旧切片元数据，不重复裁切/烧录标题或字幕；使用选中job的保存标题、版本和文件，后续修改草稿不会替换待发布文件。未完成、文件缺失/为空、跨项目/非法引用被拦截；超过目标预设时长时要求修改，不能静默截短。封面从已包装成片取帧；发布记录包含来源job和版本。兼容依赖v1.3.2发布实现，代码在集成分支收口。

离线回归52项通过（新增9项+既有发布/封面/导出），前端57项及类型/Lint/构建通过。8782浏览器通过既有26.7秒V2成片的“发布”进入现有发布页，隐藏二次字幕/标题卡设置，完成页内下载（接口200），未重新渲染、未向任何账号投稿。真实外部发布仍需明确账号和投稿授权；本轮验证只使用离线平台桩。后续继续八语、业务统计和效果/安装包验收。

## Studio统计闭环（2026-09-23）

新接口复用现有隐私开关与本机工作流观察器，记录导入/确认/导出接受或请求失败及后续终态。快速识别区分模型建议、人工回退和失败。内部标识只用于本机查询与去重；新增外部事件不含任何项目/草稿/job标识、内容、文件名或链接，仅固定结果/错误分类和请求耗时。下载事件仅表示点击。61项前端测试和类型/Lint/构建通过，未发送真实测试统计或调用模型。远程CI运行35810364706已全部通过，后续提交仍须检查各自CI。

## Studio结果与导出记录八语（2026-09-23）

结果卡片和导出历史的固定界面文案补齐中英日韩西葡俄法，使用语言切换订阅，版本/标题用插值保留，日期跟随界面语言。新增语言切换用例验证所有状态译文和标题/版本未被改写。62项前端、类型/Lint/构建通过，英文浏览器检查已导出V2历史与发布链接。此里程碑不包含编辑器/导入确认全量翻译；后台返回的警告/错误仍原样显示，不宣称Studio八语全覆盖。

## 固定界面八语收口（2026-09-23）

新增Studio界面的固定文案及外围入口、模板标签、校验提示已接入中英日韩西葡俄法，标题/模型内容不随界面语言改变。64项前端测试、类型/Lint/构建通过；实际英文界面验证了导入确认门槛、智能参数折叠、编辑器和视觉设置。服务端动态错误/警告/分析消息仍显示供应端原文，后续错误码映射需独立处理。浏览器验证未执行模型请求或写入项目。上批37f16e28远程所有CI通过。

## 缩略图、旧编辑恢复与macOS包验证（2026-09-23）

集成037fe31a/2719c217/97796628已同步功能分支缩略图缓存与恢复编辑修复，保留主线发布与八语文案。当前101项Studio/发布后端、64项前端、类型/Lint/构建通过。实际浏览器确认6张服务端JPEG返回200、内部取帧避开矿车白色转场、竖屏文字完整；旧切片封面与按钮进入同一编辑器，保存后重新打开同ID/V2/开头文本，不重复建稿。

构建2719c217的macOS ARM64包成功；SDKROOT仅本次指向本机26.5绕过SDK27链接器架构不兼容。包内Python3.13.13/FFmpeg完成六款v6图层、真实1秒竖屏渲染、隔离桌面后端health。包仍1.3.2/ad-hoc且不含后续97796628，须RC前重建，尚未签更新包/公证/测试原生窗口和升级，Windows待验。

真实347.921秒跑酷复测在第一次扫描请求90.87秒超时，整体107.6秒，无第二次请求和候选；根因为安全分类scan/timeout，不能声称筛选质量已改善。正式L2与多游戏验收保留阻塞。完整剩余清单见RELEASE_1_4最新记录。

### 2026-09-23：再次同步最新main并恢复完整CI条件

远程main已前进到a2012840（分类缺省回退、进度路径/终态轮询、Whisper运行时错误修复）；先备份远程codex/backup-1.4-before-main-a2012840@8482dfdd，再保留合并拓扑rebase。保留新增后端修复与分类控件，旧废弃进度页面不复活；Whisper CPU/VAD回退与已有真实词时间记录组合，回退成功后仍写有效sidecar，缺词时间继续保留普通SRT。

最新全量后端441项通过（隔离回归、无付费请求），补强Whisper/词时间17项复查通过；前端71项、类型/Lint/构建通过，新main的分类/轮询7项纳入npm test和CI。8份README、11份指南及社区脚本通过。之前PR显示冲突导致完整CI未触发，不能用仅文档检查冒充CI完成；rebase推送后需核对新head检查。包仍为旧2719c217构建证据，RC前必须重建。

## 终态观察与有限网络恢复（2026-09-23 下午）

功能1d765818同步为3449c31f：项目状态只有分析/导出活动任务才持续GET；确认页、完成、失败和空旧项目停止。HTTP错误停止、断网最多3次读取尝试，保留成功快照；卸载取消请求/等待，可见页面恢复和显式操作重新读取，不会触发分析。新增6项时序用例，集成77项前端及类型/Lint/生产构建通过，无后端行为变化或付费模型调用。上一候选7b6da8cb远程CI全部通过（35814941706），本提交以新CI为准。长视频扫描超时、质量评审及RC包仍未完成。

## 2026-09-23 晚：可重复包内验收与主线更新阻塞

新增scripts/verify_macos_bundle.py：强制使用app内Python并核对backend来源；每次创建独立临时数据/数据库，离线生成测试画面与声音，验证六款v6中英文字图层、1080×1920一秒渲染、原声音轨和桌面后端health。禁止写入包内pyc，退出时终止测试后端；不安装应用、不调用模型。调用示例（在仓库根目录，resources指app的Contents/Resources/resources）：

```sh
"$resources/python/bin/python3" -B scripts/verify_macos_bundle.py --resources "$resources"
```

基于a311ce23重新构建macOS ARM64包成功（含共享编辑恢复和有限轮询；生产代码对应d962beed，a311ce23新增验收脚本）。本次SDKROOT仍仅指定本机26.5，版本仍1.3.2，ad-hoc签名；447MB app、236MB DMG、160MB tar均仅本地。验收使用包内Python3.13.13/FFmpeg通过，证据/private/tmp/autoclip-bundle-smoke-x3k5kzed/report.json；之后codesign --verify --deep --strict通过。合成测试片仅验证运行时和渲染，不作为真实游戏或买量效果证据。尚未覆盖原生窗口、升级和Windows。

**当前合入阻塞：**远程main已更新到26b92494，含v1.3.3、旧枚举兼容、macOS播放/下载、导入Celery错误、字幕/模型缺失提示等61个文件变更。本次只fetch核对，未合并或rebase，避免改变构建中的基线。PR147再次显示CONFLICTING，仅文档检查通过，不能沿用7b6da8cb旧CI绿色宣称最新候选通过。下一轮优先备份候选并集成这些主线修复，重跑完整回归；本包不是最新main的RC，不用于发布。

## 2026-09-24：同步v1.3.3及后续主线修复

已远程备份codex/backup-1.4-before-main-26b92494@3387285c，并保留合并拓扑rebase到main@26b92494。纳入旧枚举迁移与兼容读取、Whisper下载控制台修复、原生播放/下载支持、Celery异常元数据、片源真实进度、字幕与模型缺失设置引导。冲突组合保留待确认导入门槛、统一Studio结果、已验证编辑恢复、文件数据库事务隔离和八语增量。候选版本随主线保留1.3.3，尚未改为1.4.0。

本地完整后端465项、前端89项、类型/Lint/生产构建、8份README与11份指南检查通过，无付费模型请求。Rust检查发现主线save_local_download返回SavedDownload私有类型导致Tauri宏编译失败，修正为crate可见后cargo check --locked通过（SDKROOT仅本次26.5）。本轮没有重建app或验证原生窗口；此前1.3.2包不是本次候选证据。

新主线旧切片系统下载路径已纳入兼容回归；Studio成片下载仍使用链接，需继续接入并实测macOS系统保存路径，不能据旧切片测试宣布两条下载体验全部收口。长视频模型超时、多游戏/L2质量、最新原生包升级与Windows仍待验。main未合并、未发布；推送后的远程CI以新head为准。

## 2026-09-24 下午：成片下载接入桌面系统保存

结果卡片、编辑器导出面板、导出记录改用同一StudioDownloadLink。浏览器保留原链接下载；Tauri桌面先等待后端地址就绪，再以指定不可变job的视频下载地址调用主线save_local_download，成功后提示下载目录。初始化超时不会向原生层提交相对/旧地址；重复点击有同步锁，失败后可重试，空文件不报成功。沿用匿名下载意图事件，不发送项目/job/文件路径等新增统计内容。此项依赖主线原生命令，因此只在集成分支实现。

前端95项、类型/Lint/生产构建通过，新增6项覆盖地址就绪、浏览器回退、桌面重复点击、空文件/原生错误、重试及地址超时；Rust原生下载3项通过，覆盖本机地址限制、中文文件名和已有文件不覆盖。本轮无模型调用、没有向实际下载目录写入测试视频。完整原生窗口点击到磁盘仍需新包实测，不将接口桩视为端到端验收；旧包不包含本修复。此前dbb226cd远程完整CI已通过（35920405695），新提交CI另看。

RC仍需：最新包/升级和Windows、Studio完整实际下载、长视频扫描timeout与真实多游戏/L2评审、服务端动态提示八语、版本与发布回退清单。未合并main、未发布。

## 2026-09-24 晚：同步 v1.3.4，解除候选冲突

候选已保留合并拓扑 rebase 到 main@d1fe99dc；远程备份 codex/backup-1.4-before-main-d1fe99dc 保存原 cffd4475。逐文件比较原候选，差异仅主线的7个文件：v1.3.4版本/变更记录与SavedDownload公开类型，Studio及确认门槛实现未丢失。保留主线pub类型，不重复覆盖为crate可见。

本轮前端95项、类型/Lint/生产构建、原生下载3项以及8份README/11份指南检查通过。后端业务代码与此前465项通过时一致，本轮未重复执行完整后端或调用模型；远程完整CI以本次新head为准。版本跟随主线保持1.3.4，未标记1.4 RC。此前1.3.2本地包不能作为当前候选验收。

9月25日RC仍有明确风险：最新原生包与实际下载/升级、Windows、动态提示八语、长视频扫描超时、多游戏真实样本及L2创意评审尚未完成。保持draft，未合并main、未打tag、未发布。
